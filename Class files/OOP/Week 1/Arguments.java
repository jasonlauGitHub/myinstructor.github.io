// Arguments

public class Arguments {
  public static void main(String[] args) {
    // Array is a list/group of things - strings
    // Arrays are position/index based data type
    System.out.println(args[0]);

    // An "argument" is a value (or values) passed to a method
    // when the method is called (main(), in this case)
  }
}

// Hello world

public class Hello {
  public static void main(String[] args) {
    // The main method() JVM use this method to initiate out program
    // Classes' names start with an uppercase letter
    // A "String" is a sequence of characters
    // 'a', for example, is a single character, not a string
    System.out.println("Hello Java");

    // A Java program begins execution with its main() method. A main() method is
    // the gateway between the startup of a Java process, which is managed by the
    // Java Virtual Machine (JVM), and the beginning of the programmer’s code. The
    // JVM calls on the underlying system to allocate memory and CPU time, access
    // files, and so on
  }
}
